import { TestBed, inject } from '@angular/core/testing';
import { SPDataService } from './sp-data.service';
import { DataComponent } from './BSDataComponent';
import { BSGlobalFilter } from './BSGlobalFilter';
/*
TODO: Implement HttpClient stuff
import { HttpClient } from '';


describe('BSDataComponent', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            providers: [SPDataService, BSGlobalFilter, HttpClient]
        });
    });

    let component: MockDataComponent;
    
    it('should be created', inject([SPDataService, BSGlobalFilter], (spData: SPDataService, globalFilter: BSGlobalFilter) => {
        component = TestBed.get(MockDataComponent);
        expect(component).toBeTruthy();
    }));
});

class MockDataComponent extends DataComponent
{
    constructor(spData: SPDataService, globalFilterService: BSGlobalFilter) { super(spData, globalFilterService); }

    protected onNewData(): void
    {
        
    }
}
*/
